a — это название массива(в первых квадратных скобках - количество строк, а во вторых — столбцов)
int a[100][100];

for (int i = 0; i < n; i++) { //перебор строк
    for (int j = 0; j < n; j++) { //перебор столбцов

